# TinyROS-Stereo

```
$ sudo apt update
$ sudo apt install cmake
$ sudo apt install libuv1-dev libssl-dev

$ cd tinyros && make

$ export PYTHONPATH=$PYTHONPATH:<path-to-tinyros>/build/output/client_library/python
$ source ~/.bashrc
```